{% extends 'admin/base.html.twig' %}

{% block title %}Skill{% endblock %}

{% block body %}
    <h1>Skill</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ skill.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ skill.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_skill_index') }}" class="btn btn-secondary">back to list</a>

    <a href="{{ path('app_admin_skill_edit', {'id': skill.id}) }}" class="btn btn-primary">edit</a>

    {{ include('admin/skill/_delete_form.html.twig') }}
{% endblock %}
