{% extends 'admin/base.html.twig' %}

{% block title %}HealthcareCenter{% endblock %}

{% block body %}
    <h1>HealthcareCenter</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ healthcare_center.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ healthcare_center.name }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ healthcare_center.slug }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ healthcare_center.phone }}</td>
            </tr>
            <tr>
                <th>PhoneEmergency</th>
                <td>{{ healthcare_center.phoneEmergency }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ healthcare_center.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_healthcare_center_index') }}">back to list</a>

    <a href="{{ path('app_admin_healthcare_center_edit', {'id': healthcare_center.id}) }}">edit</a>

    {{ include('admin/healthcare_center/_delete_form.html.twig') }}
{% endblock %}
