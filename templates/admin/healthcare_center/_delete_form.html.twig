<form method="post" action="{{ path('app_admin_healthcare_center_delete', {'id': healthcare_center.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ healthcare_center.id) }}">
    <button class="btn">Delete</button>
</form>
